spring:
  datasource:
    url: jdbc:tc:postgresql:13.3:///test_database
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  flyway:
    enabled: false